@{
    Layout = "_Layout.cshtml";

    // Authenticated HTML pages must not be retained by shared/browser caches.
    // The security response headers (CSP, HSTS, X-Frame-Options, etc.) are set
    // centrally for every response by the middleware in Program.cs.
    Context.Response.Headers["Cache-Control"] = "no-store";
}
