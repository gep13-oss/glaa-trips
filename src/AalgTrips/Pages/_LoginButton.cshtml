@{
    string path = Context.Request.Path;

    if (User.Identity.IsAuthenticated)
    {
        <a href="~/login/?returnUrl=@path&logout=1" title="Sign out">Sign out</a>
    }
    else
    {
        <a href="~/login/" title="Sign in">Sign in</a>
    }
}